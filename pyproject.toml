[project]
name = "waze-churn-analysis"
version = "0.1.0"
description = "Churn analysis on Waze data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.0",
    "ipykernel>=6.0",
]
